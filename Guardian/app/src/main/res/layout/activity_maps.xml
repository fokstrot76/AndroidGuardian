<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_height="match_parent"
    android:layout_width="match_parent"
    xmlns:map="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
<fragment
    android:id="@+id/map"
    android:name="com.google.android.gms.maps.SupportMapFragment"
    android:layout_width="match_parent"
    android:layout_height="300sp"
    tools:context="com.example.piotr.guardian.MapsActivity"
    android:layout_below="@+id/buttonEmergency"
    android:layout_above="@+id/address" />
    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/buttonPupil"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_margin="10sp"
        android:background="#1c32f7"
        android:textColor="#ffffff"
        android:textStyle="bold"
        android:textSize="20sp"
        android:onClick="callPupil" />

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/buttonEmergency"
        android:layout_below="@+id/buttonPupil"
        android:layout_centerHorizontal="true"
        android:text="@string/CallEmergency"
        android:textSize="20sp"
        android:textStyle="bold"
        android:background="#fa3131"
        android:layout_margin="10sp"
        android:onClick="callEmergency" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/address"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:textColor="#000000"
        android:textSize="25sp" />
</RelativeLayout>
